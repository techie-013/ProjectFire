import 'package:flutter/material.dart';


class WasteEntryCard extends StatelessWidget {
  const WasteEntryCard({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}