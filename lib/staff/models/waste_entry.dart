import 'package:flutter/material.dart';


class WasteEntry extends StatelessWidget {
  const WasteEntry({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}