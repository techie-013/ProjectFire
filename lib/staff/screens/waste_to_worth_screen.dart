import 'package:flutter/material.dart';


class WasteToWorthScreen extends StatelessWidget {
  const WasteToWorthScreen({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Waste To Worth')),
      body: Center(child: Text('Excess Food → NGOs – Coming Soon')),
    );
  }
}