import 'package:flutter/material.dart';


class PollService extends StatelessWidget {
  const PollService({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}