import 'package:flutter/material.dart';


class BillTitle extends StatelessWidget {
  const BillTitle({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}