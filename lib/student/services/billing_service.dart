import 'package:flutter/material.dart';


class BillingService extends StatelessWidget {
  const BillingService({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}