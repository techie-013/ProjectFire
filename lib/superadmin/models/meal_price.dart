import 'package:flutter/material.dart';


class MealPrice extends StatelessWidget {
  const MealPrice({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}