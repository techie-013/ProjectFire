import 'package:flutter/material.dart';


class PricingService extends StatelessWidget {
  const PricingService({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}