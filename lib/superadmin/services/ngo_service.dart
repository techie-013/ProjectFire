import 'package:flutter/material.dart';


class NgoService extends StatelessWidget {
  const NgoService({super.key});


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coming Soon')),
    );
  }
}